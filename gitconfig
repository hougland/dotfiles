# This is Git's per-user configuration file.
[user]
# Please adapt and uncomment the following lines:
	name = Ricky Hougland
	email = ricky.hougland@socrata.com

# Git tree
[alias]
        tree = log --graph --decorate --pretty=oneline --abbrev-commit
